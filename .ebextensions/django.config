option_settings:
  aws:elasticbeanstalk:container:python:
    WSGIPath: Cloud.wsgi:application
  aws:elasticbeanstalk:environment:proxy:staticfiles:
    /static: static

commands:
  install_packages:
    - command: pip install opencv-python-headless
    - command: sudo yum install -y dbus-devel dbus-glib-devel
    - command: sudo yum install -y libGL
